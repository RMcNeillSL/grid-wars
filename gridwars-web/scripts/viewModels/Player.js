define([
], function () {
    'use strict';

    var Player = function Player(id, name) {
        this.id = id;
        this.name = name;
    };
    Player.prototype = {
    };

    return Player;
});